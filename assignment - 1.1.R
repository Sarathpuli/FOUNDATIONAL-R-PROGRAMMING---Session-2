1.	i. True
  ii. True

2.	Recycling elements are the shortest size vector in a computation. the values inside the vector keep on repeating until the particular operation completes.

3.	Ex: RV <- c(1,2,3,4,5,6) + c(1,2)  here c(1,2) is the recycling vector
